(function ( $ ) {
"use strict";
$(function () {
if ( 0 < $('form#post[action="comment.php"]').length ) $( 'form#post[action="comment.php"]'  ).attr( 'enctype', 'multipart/form-data' );
if ( 0 === $( '#disable_comment_images' ).length ) return;
$( '#comment_image_toggle' ).on( 'click', function () {
if ( confirm( cm_imgs.toggleConfirm ) ) {
$( this).attr( 'disabled', 'disabled' );
$( '#comment_image_source' ).val( 'button' );
$( '#publish' ).trigger( 'click' );
}
});
});
})( jQuery );
